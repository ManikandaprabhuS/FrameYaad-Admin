import React from 'react';

const CheckoutPage: React.FC = () => (
  <div className="rounded-[2rem] border border-outline-variant bg-surface-container-lowest p-8">
    <h1 className="text-3xl font-black text-on-surface">Checkout</h1>
    <p className="mt-2 text-sm text-on-surface-variant">Checkout will reuse the existing payment and order APIs when connected.</p>
  </div>
);

export default CheckoutPage;
